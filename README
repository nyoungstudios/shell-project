Nathaniel Young
young410@purdue.edu
Shell Project

1. Features specified in the handout that work.
	everything works
	highlights to test:
		history feature
		editing history commands
		.shellrc file
		tilde expansion
		source with environment variables
		

2. Features specified in the handout that do not work.
	nothing

3. Extra features I have implemented.
	cd -
		This returns to the last directory the user was in before the current directory. Also updates PWD and OLDPWD environment variables for all cd commands.
	$?, $_, $SHELL, $anything
		Extra environment variable parsing without the curly brackets like zsh.
	error statement if command not found

